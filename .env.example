# SQLite - no database server required (file is created in prisma/)
DATABASE_URL="file:./burms.db"

JWT_SECRET="change-this-to-a-secure-random-string-in-production"
NEXT_PUBLIC_APP_URL="http://localhost:3001"
APP_NAME="MAGS Resource Management System - UBLC"

# Optional: enable email notifications on approval/decline
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
# SMTP_SECURE=false
# EMAIL_FROM="noreply@mags.local"
